{
  "name": "@davstack/cli-utils",
  "version": "1.1.0",
  "license": "MIT",
  "type": "module",
  "description": "Zero-dep CLI helper shared by @davstack/logs-server, vitest-server, playwright-server.",
  "exports": {
    ".": "./dist/cli.js",
    "./help": "./dist/cli-help.js",
    "./config": "./dist/config.js"
  },
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "tsup",
    "test": "pnpm -w exec vitest run --project cli-utils"
  },
  "files": [
    "dist/**"
  ],
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
